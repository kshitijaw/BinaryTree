This repository contains the codes for construction of binary tree and BFS traversal of binary tree. There are two codes for BFS, one is with the use of recurrsion and the second one using queue data structure.

